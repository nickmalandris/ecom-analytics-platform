{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
      "builder": "DOCKERFILE",
      "dockerfilePath": "Dockerfile.frontend"
    },
    "deploy": {
      "startCommand": "sh -c 'npm run start -- --port ${PORT} --host 0.0.0.0'",
      "healthcheckPath": "/",
      "restartPolicyType": "ON_FAILURE"
    }
}